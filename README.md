# 学习番茄时钟

一个使用 React + Vite + TypeScript 实现的纯前端学习计时网页应用。

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## GitHub Pages 部署

仓库推送到 GitHub 后，在仓库 Settings -> Pages 中把 Source 设置为 GitHub Actions。
之后每次推送到 `main` 分支都会自动构建并部署。
